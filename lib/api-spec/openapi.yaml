openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: Beach Volleyball Manager API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: youth-scouting
    description: Youth Academy scouting missions
  - name: auth
    description: Authentication
  - name: players
    description: Player management
  - name: teams
    description: Team management
  - name: matches
    description: Match management
  - name: seasons
    description: Season and ladder
  - name: contracts
    description: Player contracts
  - name: staff
    description: Staff and coaches
  - name: training
    description: Training sessions
  - name: finances
    description: Financial management
  - name: draft
    description: Player draft system
  - name: outfits
    description: Player outfits
  - name: locations
    description: Game locations
  - name: leaderboard
    description: Leaderboard
  - name: olympics
    description: Olympic Games management
  - name: medical
    description: Medical Centre and injury statistics

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/user:
    get:
      operationId: getCurrentAuthUser
      tags: [auth]
      summary: Get current authenticated user
      responses:
        "200":
          description: Current user info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthUser"
        "401":
          description: Not authenticated

  /profile:
    get:
      operationId: getProfile
      tags: [auth]
      summary: Get user game profile
      responses:
        "200":
          description: User profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Not authenticated
    post:
      operationId: createProfile
      tags: [auth]
      summary: Create user game profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserProfileInput"
      responses:
        "201":
          description: Profile created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
    patch:
      operationId: updateProfile
      tags: [auth]
      summary: Update user game profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserProfileUpdate"
      responses:
        "200":
          description: Profile updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"

  /locations:
    get:
      operationId: listLocations
      tags: [locations]
      summary: List all world locations
      responses:
        "200":
          description: All locations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Location"

  /locations/{id}:
    get:
      operationId: getLocation
      tags: [locations]
      summary: Get a single location
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Location detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Location"

  /players:
    get:
      operationId: listPlayers
      tags: [players]
      summary: List all players for the user's team
      responses:
        "200":
          description: Player list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Player"
    post:
      operationId: createPlayer
      tags: [players]
      summary: Create a player (admin/dev use)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlayerInput"
      responses:
        "201":
          description: Player created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"

  /players/free-agents:
    get:
      operationId: listFreeAgents
      tags: [players]
      summary: List players without a team (available to sign)
      responses:
        "200":
          description: Free agents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Player"

  /players/{id}:
    get:
      operationId: getPlayer
      tags: [players]
      summary: Get player detail
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Player
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"
    patch:
      operationId: updatePlayer
      tags: [players]
      summary: Update player data
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlayerUpdate"
      responses:
        "200":
          description: Updated player
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"

  /players/{id}/outfit:
    patch:
      operationId: updatePlayerOutfit
      tags: [players]
      summary: Change a player's outfit
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OutfitAssignment"
      responses:
        "200":
          description: Outfit updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"

  /players/{id}/release:
    post:
      operationId: releasePlayer
      tags: [players]
      summary: Release a player from the team
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Player released
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"

  /players/{id}/retire:
    post:
      operationId: retirePlayer
      tags: [players]
      summary: Retire a player and add them to the Hall of Fame
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Player retired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"
        "403":
          description: Not your player
        "404":
          description: Player not found

  /players/{id}/training-focus:
    patch:
      operationId: setPlayerTrainingFocus
      tags: [players]
      summary: Set or clear a youth player's training focus
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetTrainingFocusBody"
      responses:
        "200":
          description: Updated player
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"
        "400":
          description: Not a youth player (must be age 14–18)
        "403":
          description: Not your player
        "404":
          description: Player not found

  /players/{id}/scout:
    post:
      operationId: scoutPlayer
      tags: [players]
      summary: Have your scout assess a player's hidden potential
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Scouting report with assessed potential
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScoutingResult"
        "400":
          description: No scout on staff

  /outfits:
    get:
      operationId: listOutfits
      tags: [outfits]
      summary: List all available outfits
      responses:
        "200":
          description: Outfits
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Outfit"

  /team:
    get:
      operationId: getMyTeam
      tags: [teams]
      summary: Get the current user's team
      responses:
        "200":
          description: Team info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Team"
        "404":
          description: No team yet
    post:
      operationId: createTeam
      tags: [teams]
      summary: Create the user's team
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TeamInput"
      responses:
        "201":
          description: Team created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Team"
    patch:
      operationId: updateTeam
      tags: [teams]
      summary: Update team info
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TeamUpdate"
      responses:
        "200":
          description: Updated team
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Team"

  /team/roster:
    get:
      operationId: getTeamRoster
      tags: [teams]
      summary: Get team roster with player details
      responses:
        "200":
          description: Roster with players and staff
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamRoster"

  /team/swap-player:
    post:
      operationId: swapTeamPlayer
      tags: [teams]
      summary: Swap a player in/out of the active lineup
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlayerSwap"
      responses:
        "200":
          description: Swap confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamRoster"

  /team/roster/{id}/role:
    patch:
      operationId: setPlayerRole
      tags: [teams]
      summary: Set a player's squad role (starter / interchange / reserve)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlayerRoleUpdate"
      responses:
        "200":
          description: Role updated, full roster returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamRoster"

  /contracts:
    get:
      operationId: listContracts
      tags: [contracts]
      summary: List all active contracts for the user's team
      responses:
        "200":
          description: Contracts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Contract"
    post:
      operationId: signContract
      tags: [contracts]
      summary: Sign a player to a contract (max 1 year)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContractInput"
      responses:
        "201":
          description: Contract signed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contract"

  /contracts/{id}:
    get:
      operationId: getContract
      tags: [contracts]
      summary: Get a single contract
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Contract
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contract"
    delete:
      operationId: terminateContract
      tags: [contracts]
      summary: Terminate a contract early
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Contract terminated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contract"

  /staff:
    get:
      operationId: listStaff
      tags: [staff]
      summary: List the user's team staff
      responses:
        "200":
          description: Staff list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StaffMember"
    post:
      operationId: hireStaff
      tags: [staff]
      summary: Hire a staff member
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffInput"
      responses:
        "201":
          description: Staff hired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"

  /staff/available:
    get:
      operationId: listAvailableStaff
      tags: [staff]
      summary: List staff available to hire
      responses:
        "200":
          description: Available staff
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StaffMember"

  /staff/market:
    get:
      operationId: getStaffMarket
      tags: [staff]
      summary: Browse staff available on the market with optional filters
      parameters:
        - name: role
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Staff market list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StaffMember"

  /staff/{id}:
    delete:
      operationId: fireStaff
      tags: [staff]
      summary: Fire a staff member
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Staff fired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"

  /staff/{id}/scout:
    post:
      operationId: scoutStaff
      tags: [staff]
      summary: Reveal a staff member's OVR and attributes using a scout
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Staff OVR revealed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"
        "400":
          description: No scout available or already revealed

  /training:
    get:
      operationId: listTrainingSessions
      tags: [training]
      summary: List scheduled training sessions
      responses:
        "200":
          description: Training sessions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TrainingSession"
    post:
      operationId: scheduleTraining
      tags: [training]
      summary: Schedule a training session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TrainingInput"
      responses:
        "201":
          description: Training scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrainingSession"

  /training/{id}/complete:
    post:
      operationId: completeTraining
      tags: [training]
      summary: Mark training as complete and apply stat gains
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Training completed with stat gains
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrainingResult"

  /training/team:
    post:
      operationId: scheduleTeamTraining
      tags: [training]
      summary: Schedule a training session for the entire active roster
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TeamTrainingInput"
      responses:
        "201":
          description: Team training sessions created (one per active player)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TrainingSession"

  /training/plan:
    get:
      operationId: getTrainingPlan
      tags: [training]
      summary: Get weekly training plan summary with morale and fitness
      responses:
        "200":
          description: Training plan overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrainingPlan"

  /matches:
    get:
      operationId: listMatches
      tags: [matches]
      summary: List all matches for the current season
      responses:
        "200":
          description: Match list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Match"
    post:
      operationId: scheduleMatch
      tags: [matches]
      summary: Schedule a match
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MatchInput"
      responses:
        "201":
          description: Match scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Match"

  /matches/{id}:
    get:
      operationId: getMatch
      tags: [matches]
      summary: Get match detail including weather and score
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Match detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Match"

  /matches/{id}/simulate:
    post:
      operationId: simulateMatch
      tags: [matches]
      summary: Simulate a match and get result
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Match result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchResult"

  /matches/{id}/lineup:
    patch:
      operationId: updateMatchLineup
      tags: [matches]
      summary: Update player lineup before or during a match
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LineupUpdate"
      responses:
        "200":
          description: Lineup updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Match"

  /matches/upcoming:
    get:
      operationId: listUpcomingMatches
      tags: [matches]
      summary: Get next 5 upcoming matches with weather forecast
      responses:
        "200":
          description: Upcoming matches
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Match"

  /seasons:
    get:
      operationId: listSeasons
      tags: [seasons]
      summary: List all seasons
      responses:
        "200":
          description: Seasons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Season"
    post:
      operationId: startSeason
      tags: [seasons]
      summary: Start a new season
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeasonInput"
      responses:
        "201":
          description: Season started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Season"

  /seasons/current:
    get:
      operationId: getCurrentSeason
      tags: [seasons]
      summary: Get the current active season
      responses:
        "200":
          description: Current season
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Season"

  /seasons/{id}/ladder:
    get:
      operationId: getSeasonLadder
      tags: [seasons]
      summary: Get the season standings ladder
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Season ladder
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LadderEntry"

  /finances:
    get:
      operationId: listFinances
      tags: [finances]
      summary: List all financial transactions for the user's team
      responses:
        "200":
          description: Transactions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FinanceTransaction"
    post:
      operationId: addFinanceTransaction
      tags: [finances]
      summary: Add a financial transaction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinanceTransactionInput"
      responses:
        "201":
          description: Transaction recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceTransaction"

  /finances/summary:
    get:
      operationId: getFinanceSummary
      tags: [finances]
      summary: Get financial summary with totals, income breakdown, expenses
      responses:
        "200":
          description: Finance summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceSummary"

  /finances/sponsor-reputation:
    get:
      operationId: getSponsorReputation
      tags: [finances]
      summary: Get team sponsor reputation score and rating label
      responses:
        "200":
          description: Sponsor reputation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SponsorReputation"

  /finances/staff-wage-bill:
    get:
      operationId: getStaffWageBill
      tags: [finances]
      summary: Get weekly and monthly staff wage totals with per-staff breakdown
      responses:
        "200":
          description: Staff wage bill summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffWageBill"

  /finances/wage-bill:
    get:
      operationId: getWageBill
      tags: [finances]
      summary: Get weekly and monthly player wage totals with per-player breakdown
      responses:
        "200":
          description: Wage bill summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WageBill"

  /finances/prize-money:
    get:
      operationId: getPrizeMoneySummary
      tags: [finances]
      summary: Get season prize money earned grouped by tour category
      responses:
        "200":
          description: Prize money summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrizeMoneySummary"

  /finances/sponsor-progress:
    get:
      operationId: getSponsorProgress
      tags: [finances]
      summary: Get accepted sponsor deals with current win progress
      responses:
        "200":
          description: Sponsor progress list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SponsorProgress"

  /finances/promo-deals:
    get:
      operationId: listPromoDeals
      tags: [finances]
      summary: List available sponsorship and promo deals
      responses:
        "200":
          description: Promo deals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PromoDeal"

  /finances/promo-deals/{id}/accept:
    post:
      operationId: acceptPromoDeal
      tags: [finances]
      summary: Accept a promo/sponsorship deal
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deal accepted and income recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceTransaction"

  /draft:
    get:
      operationId: getDraftPool
      tags: [draft]
      summary: Get current draft pool of available players
      responses:
        "200":
          description: Draft pool
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DraftPlayer"

  /draft/generate-class:
    post:
      operationId: generateDraftClass
      tags: [draft]
      summary: Generate a new draft class (quality influenced by Youth Academy level)
      responses:
        "200":
          description: New draft class
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DraftPlayer"
        "401":
          description: Unauthorized

  /draft/pick:
    post:
      operationId: draftPick
      tags: [draft]
      summary: Draft a player from the pool
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DraftPickInput"
      responses:
        "201":
          description: Player drafted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"

  /leaderboard:
    get:
      operationId: getLeaderboard
      tags: [leaderboard]
      summary: Get global leaderboard across all users/teams
      responses:
        "200":
          description: Leaderboard
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaderboardEntry"

  /dashboard:
    get:
      operationId: getDashboard
      tags: [teams]
      summary: Get dashboard summary (team stats, next match, finances, recent results)
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Dashboard"

  /olympics/countries:
    get:
      operationId: listOlympicCountries
      tags: [olympics]
      summary: List eligible Olympic countries with their squads
      responses:
        "200":
          description: List of eligible countries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OlympicCountry"
        "401":
          description: Unauthorized

  /olympics/selection:
    get:
      operationId: getOlympicSelection
      tags: [olympics]
      summary: Get the current user's Olympic country selection
      responses:
        "200":
          description: Current selection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OlympicSelection"
        "401":
          description: Unauthorized
        "404":
          description: No selection made yet
    post:
      operationId: saveOlympicSelection
      tags: [olympics]
      summary: Save the user's Olympic country selection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OlympicSelectionInput"
      responses:
        "200":
          description: Selection saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OlympicSelection"
        "400":
          description: Invalid input
        "401":
          description: Unauthorized
    delete:
      operationId: clearOlympicSelection
      tags: [olympics]
      summary: Clear the user's Olympic selection and remove all wildcard players
      description: >
        Called when the Olympic tournament concludes. Deletes the entire
        olympic_selections row for the user. Wildcard players (id: null) exist
        only inside this row's squad JSON and are permanently removed with it.
        They are never written to playersTable, contractsTable, draftTable, or
        any club roster.
      responses:
        "204":
          description: Selection cleared
        "401":
          description: Unauthorized
        "404":
          description: No selection to clear

  /facilities:
    get:
      operationId: getFacilities
      tags: [facilities]
      summary: Get all facilities for the team (auto-created at level 1 if missing)
      responses:
        "200":
          description: List of facilities
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Facility"
        "401":
          description: Unauthorized

  /facilities/{type}/upgrade:
    post:
      operationId: upgradeFacility
      tags: [facilities]
      summary: Upgrade a facility to the next level
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Upgraded facility
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Facility"
        "400":
          description: Already at max level or insufficient budget
        "401":
          description: Unauthorized
        "404":
          description: Facility not found

  /club-rating:
    get:
      operationId: getClubRating
      tags: [facilities]
      summary: Get the club overall rating with weighted breakdown
      responses:
        "200":
          description: Club rating breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClubRating"
        "401":
          description: Unauthorized
        "404":
          description: Team not found

  /achievements:
    get:
      operationId: getAchievements
      tags: [achievements]
      summary: Get all achievements with unlock status and progress for the current team
      responses:
        "200":
          description: Achievement list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AchievementStatus"
        "401":
          description: Unauthorized
        "404":
          description: Team not found

  /achievements/career-stats:
    get:
      operationId: getCareerStats
      tags: [achievements]
      summary: Get raw career stats for the current team
      responses:
        "200":
          description: Career stats object
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CareerStats"
        "401":
          description: Unauthorized
        "404":
          description: Team not found

  /trophies/hall-of-fame:
    get:
      operationId: getHallOfFame
      tags: [trophies]
      summary: Get all retired players in the Hall of Fame
      responses:
        "200":
          description: Hall of Fame entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HallOfFameEntry"
        "401":
          description: Unauthorized

  /trophies/cabinet:
    get:
      operationId: getTrophyCabinet
      tags: [trophies]
      summary: Get the manager's full trophy cabinet and career achievements
      responses:
        "200":
          description: Trophy cabinet data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrophyCabinet"
        "401":
          description: Unauthorized
        "404":
          description: Team not found

  /medical-staff:
    get:
      operationId: listMedicalStaff
      tags: [medical]
      summary: List the user's hired medical staff
      responses:
        "200":
          description: Medical staff list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StaffMember"
        "401":
          description: Unauthorized
    post:
      operationId: hireMedicalStaff
      tags: [medical]
      summary: Hire a medical staff member
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [staffId]
              properties:
                staffId:
                  type: integer
      responses:
        "201":
          description: Hired medical staff member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"
        "400":
          description: Staff full or already hired
        "401":
          description: Unauthorized

  /medical-staff/market:
    get:
      operationId: getMedicalStaffMarket
      tags: [medical]
      summary: Browse medical staff available on the market
      parameters:
        - in: query
          name: role
          schema:
            type: string
          description: Filter by medical role
        - in: query
          name: search
          schema:
            type: string
          description: Search by name, specialty, nationality, or trait
      responses:
        "200":
          description: Medical staff market list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StaffMember"

  /medical-staff/{id}:
    delete:
      operationId: fireMedicalStaff
      tags: [medical]
      summary: Release a medical staff member
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Released medical staff member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"
        "401":
          description: Unauthorized

  /medical/injury-history:
    get:
      operationId: getInjuryHistory
      tags: [medical]
      summary: Get season injury history for the current team (newest first)
      responses:
        "200":
          description: List of injury history entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InjuryHistoryEntry"
        "401":
          description: Unauthorized
        "404":
          description: No team found

  /medical/workload:
    get:
      operationId: getPlayerWorkloads
      tags: [medical]
      summary: Get workload status for all players (last 14 days)
      responses:
        "200":
          description: Per-player workload data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlayerWorkload"
        "401":
          description: Unauthorized
        "404":
          description: No team found

  /medical/injury-stats:
    get:
      operationId: getSeasonInjuryStats
      tags: [medical]
      summary: Get season injury statistics for the current team
      responses:
        "200":
          description: Season injury statistics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SeasonInjuryStats"
        "401":
          description: Unauthorized
        "404":
          description: No team found

  /wellbeing/status:
    get:
      operationId: getWellbeingStatus
      tags: [wellbeing]
      summary: Get active wellbeing effects and team budget
      responses:
        "200":
          description: Wellbeing status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WellbeingStatus"
        "401":
          description: Unauthorized

  /wellbeing/run:
    post:
      operationId: runWellbeingCamp
      tags: [wellbeing]
      summary: Run a wellbeing camp for the team
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RunCampInput"
      responses:
        "200":
          description: Camp completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WellbeingResult"
        "400":
          description: Invalid camp type or insufficient funds
        "401":
          description: Unauthorized

  /youth-league/results:
    get:
      operationId: getYouthLeagueResults
      tags: [youth-scouting]
      summary: Get recent Youth Development League results for the team's youth players
      responses:
        "200":
          description: Youth league results grouped by week
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/YouthLeagueResult"
        "401":
          description: Unauthorized

  /youth-league/ladder:
    get:
      operationId: getYouthLadder
      tags: [youth-scouting]
      summary: Get the Youth Development League ladder for the current season
      responses:
        "200":
          description: Ladder standings sorted by points descending
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/YouthLadderEntry"
        "401":
          description: Unauthorized

  /youth-league/stars:
    get:
      operationId: getYouthStars
      tags: [youth-scouting]
      summary: Get top youth players (ages 14-18) ordered by rating
      responses:
        "200":
          description: Top youth players
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/YouthStar"
        "401":
          description: Unauthorized

  /youth-league/championship:
    get:
      operationId: getYouthChampionship
      tags: [youth-scouting]
      summary: Get Youth Championship trophy history
      responses:
        "200":
          description: Youth championship trophy history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/YouthChampionshipTrophy"
        "401":
          description: Unauthorized

  /youth-scouting:
    get:
      operationId: getYouthScouting
      tags: [youth-scouting]
      summary: Get current youth scouting mission status
      responses:
        "200":
          description: Current scouting mission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/YouthScoutingMission"
        "401":
          description: Unauthorized
        "404":
          description: No team found

  /youth-scouting/start:
    post:
      operationId: startYouthScouting
      tags: [youth-scouting]
      summary: Start a youth scouting mission
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartYouthScoutingInput"
      responses:
        "201":
          description: Mission started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/YouthScoutingMission"
        "400":
          description: Invalid continent
        "401":
          description: Unauthorized
        "422":
          description: Mission already in progress

  /youth-scouting/cancel:
    post:
      operationId: cancelYouthScouting
      tags: [youth-scouting]
      summary: Cancel the current scouting mission
      responses:
        "200":
          description: Mission cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/YouthScoutingMission"
        "401":
          description: Unauthorized

  /youth-scouting/prospects:
    get:
      operationId: getYouthProspects
      tags: [youth-scouting]
      summary: List pending scouted prospects
      responses:
        "200":
          description: Array of pending prospects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/YouthProspect"
        "401":
          description: Unauthorized

  /youth-scouting/prospects/{id}/sign:
    post:
      operationId: signYouthProspect
      tags: [youth-scouting]
      summary: Reserve a scouted prospect for signing
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Prospect reserved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/YouthProspect"
        "401":
          description: Unauthorized
        "404":
          description: Prospect not found
        "422":
          description: Prospect no longer available

  /youth-scouting/prospects/{id}/ignore:
    post:
      operationId: ignoreYouthProspect
      tags: [youth-scouting]
      summary: Dismiss a scouted prospect
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Prospect dismissed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/YouthProspect"
        "401":
          description: Unauthorized
        "404":
          description: Prospect not found

  /continental-scouting/regions:
    get:
      operationId: getContinentalRegions
      tags: [continental-scouting]
      summary: Get all scouting regions with current mission status
      responses:
        "200":
          description: Array of 6 regions with optional active mission
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContinentalRegion"
        "401":
          description: Unauthorized

  /continental-scouting/start:
    post:
      operationId: startContinentalScouting
      tags: [continental-scouting]
      summary: Start a continental scouting mission
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartContinentalScoutingInput"
      responses:
        "201":
          description: Mission created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContinentalScoutingMission"
        "400":
          description: Invalid region or duration
        "401":
          description: Unauthorized
        "422":
          description: Mission already active or insufficient funds

  /continental-scouting/missions/{id}/collect:
    post:
      operationId: collectContinentalMission
      tags: [continental-scouting]
      summary: Collect prospects from a completed mission
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Prospects collected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CollectMissionResult"
        "401":
          description: Unauthorized
        "404":
          description: Mission not found
        "422":
          description: Mission not ready to collect

  /continental-scouting/missions/{id}/cancel:
    post:
      operationId: cancelContinentalMission
      tags: [continental-scouting]
      summary: Cancel an active continental scouting mission
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Mission cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContinentalScoutingMission"
        "401":
          description: Unauthorized
        "404":
          description: Mission not found
        "422":
          description: Not an active mission

  /continental-scouting/missions/{id}/dev-complete:
    post:
      operationId: devCompleteContinentalMission
      tags: [continental-scouting]
      summary: Force-complete a mission for testing
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Mission force-completed
        "401":
          description: Unauthorized

  /continental-scouting/prospects:
    get:
      operationId: getContinentalProspects
      tags: [continental-scouting]
      summary: Get pending prospects from continental missions
      responses:
        "200":
          description: Array of continental prospects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/YouthProspect"
        "401":
          description: Unauthorized

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    AuthUser:
      type: object
      required: [id, username]
      properties:
        id:
          type: string
        username:
          type: string
        profileImage:
          type: ["string", "null"]
        hasProfile:
          type: boolean

    UserProfile:
      type: object
      required: [id, userId, teamName, totalWins, totalLosses, totalEarnings, createdAt]
      properties:
        id:
          type: integer
        userId:
          type: string
        teamName:
          type: string
        totalWins:
          type: integer
        totalLosses:
          type: integer
        totalEarnings:
          type: number
        createdAt:
          type: string

    UserProfileInput:
      type: object
      required: [teamName]
      properties:
        teamName:
          type: string

    UserProfileUpdate:
      type: object
      properties:
        teamName:
          type: string

    Location:
      type: object
      required: [id, name, country, city, description, weatherPatterns, imageUrl, courtType]
      properties:
        id:
          type: integer
        name:
          type: string
        country:
          type: string
        city:
          type: string
        description:
          type: string
        weatherPatterns:
          type: array
          items:
            type: string
        imageUrl:
          type: string
        courtType:
          type: string
        latitude:
          type: ["number", "null"]
        longitude:
          type: ["number", "null"]

    Player:
      type: object
      required: [id, name, nationality, age, height, position, speed, power, defense, serve, block, stamina, morale, fatigue, fitness, injuryStatus, injuryWeeksRemaining, consecutiveMatchesPlayed, doctorQuality, trainingPoints, salary, teamId, outfitId, isActive, imageUrl, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        nationality:
          type: string
        age:
          type: integer
        height:
          type: number
        position:
          type: string
          enum: [setter, spiker, defender, blocker, server, all_rounder]
        speed:
          type: integer
        power:
          type: integer
        defense:
          type: integer
        serve:
          type: integer
        block:
          type: integer
        stamina:
          type: integer
        morale:
          type: integer
        fatigue:
          type: integer
          description: Player fatigue level 0-100. High fatigue reduces performance.
        fitness:
          type: integer
          description: Player fitness level 0-100. Drops with matches and training; recovers with rest.
        injuryStatus:
          type: string
          enum: [Healthy, Minor Injury, Major Injury, Unavailable]
          description: Current injury state of the player.
        injuryWeeksRemaining:
          type: integer
          description: Number of weeks until the player recovers from injury. 0 when healthy.
        consecutiveMatchesPlayed:
          type: integer
          description: Number of matches played back-to-back without rest. High values increase injury risk.
        doctorQuality:
          type: integer
          minimum: 1
          maximum: 5
          description: Quality of medical care assigned to this player (1 = basic, 5 = elite). Affects recovery speed.
        trainingPoints:
          type: integer
          description: Accumulated training XP. Every 100 points converts to +1 in the trained stat. Never resets between seasons.
        salary:
          type: number
        teamId:
          type: ["integer", "null"]
        outfitId:
          type: ["integer", "null"]
        isActive:
          type: boolean
        squadRole:
          type: string
          enum: [starter, interchange, reserve]
          description: "Player's squad role: starter (match player), interchange (bench sub), or reserve."
        imageUrl:
          type: ["string", "null"]
        contractEndDate:
          type: ["string", "null"]
        scoutedPotential:
          type: ["string", "null"]
          enum: ["Low", "Average", "High", "Elite", "Generational", null]
          description: "Scout's assessment of player potential. Null if the player has not been scouted yet."
        discoveredBy:
          type: ["string", "null"]
          description: "Scouting source, e.g. 'Discovered in South America by Scout Birgit Hansen'"
        eliteEventType:
          type: ["string", "null"]
          enum: ["Generational Talent", "Olympic Wonderkid", "Physical Freak", "Local Hero", null]
          description: "Rare elite scouting event classification, if applicable."
        trainingFocus:
          type: ["string", "null"]
          enum: ["Attack", "Defence", "Serving", "Blocking", "Athleticism", "Leadership", null]
          description: "Youth player's assigned training focus. Only applies to players aged 14–18. Null if no focus is set."
        focusXp:
          type: integer
          description: "Accumulated XP toward the next focus stat gain. Every 100 points converts to +1 in the focus stat."
        academyContractYears:
          type: ["number", "null"]
          description: "Years remaining on the player's academy contract. Null for senior players. Decrements by 1/52 each match week."
        createdAt:
          type: string

    PlayerInput:
      type: object
      required: [name, nationality, age, height, position, speed, power, defense, serve, block, stamina, salary]
      properties:
        name:
          type: string
        nationality:
          type: string
        age:
          type: integer
        height:
          type: number
        position:
          type: string
        speed:
          type: integer
        power:
          type: integer
        defense:
          type: integer
        serve:
          type: integer
        block:
          type: integer
        stamina:
          type: integer
        salary:
          type: number

    PlayerUpdate:
      type: object
      properties:
        name:
          type: string
        isActive:
          type: boolean
        morale:
          type: integer

    OutfitAssignment:
      type: object
      required: [outfitId]
      properties:
        outfitId:
          type: integer

    Outfit:
      type: object
      required: [id, name, primaryColor, secondaryColor, description, price, imageUrl]
      properties:
        id:
          type: integer
        name:
          type: string
        primaryColor:
          type: string
        secondaryColor:
          type: string
        description:
          type: string
        price:
          type: number
        imageUrl:
          type: ["string", "null"]

    Team:
      type: object
      required: [id, userId, name, budget, reputation, wins, losses, titlesWon, locationId, createdAt, managerRepPoints, winStreak]
      properties:
        id:
          type: integer
        userId:
          type: string
        name:
          type: string
        budget:
          type: number
        reputation:
          type: integer
        wins:
          type: integer
        losses:
          type: integer
        titlesWon:
          type: integer
        locationId:
          type: ["integer", "null"]
        logoColor:
          type: ["string", "null"]
        trainingPhilosophy:
          type: ["string", "null"]
          enum: ["Power Volleyball", "Fast Volleyball", "Defensive Volleyball", null]
          description: "Team-wide training philosophy; provides small XP bonuses to related programs."
        managerRepPoints:
          type: integer
          description: "Manager reputation XP — used to derive level (1–5) and progress to next level"
        winStreak:
          type: integer
          description: "Current consecutive-win streak"
        createdAt:
          type: string

    TeamInput:
      type: object
      required: [name, locationId]
      properties:
        name:
          type: string
        locationId:
          type: integer
        logoColor:
          type: string

    TeamUpdate:
      type: object
      properties:
        name:
          type: string
        locationId:
          type: integer
        logoColor:
          type: string
        trainingPhilosophy:
          type: ["string", "null"]
          enum: ["Power Volleyball", "Fast Volleyball", "Defensive Volleyball", null]

    TeamRoster:
      type: object
      required: [team, starters, interchanges, reserves, activePlayers, benchPlayers, staff]
      properties:
        team:
          $ref: "#/components/schemas/Team"
        starters:
          type: array
          items:
            $ref: "#/components/schemas/Player"
        interchanges:
          type: array
          items:
            $ref: "#/components/schemas/Player"
        reserves:
          type: array
          items:
            $ref: "#/components/schemas/Player"
        activePlayers:
          type: array
          description: "Legacy: starters + interchanges combined."
          items:
            $ref: "#/components/schemas/Player"
        benchPlayers:
          type: array
          description: "Legacy: same as reserves."
          items:
            $ref: "#/components/schemas/Player"
        staff:
          type: array
          items:
            $ref: "#/components/schemas/StaffMember"

    SetTrainingFocusBody:
      type: object
      required: [focus]
      properties:
        focus:
          type: string
          description: "Training focus to assign. One of: Attack, Defence, Serving, Blocking, Athleticism, Leadership. Send empty string to clear."

    PlayerRoleUpdate:
      type: object
      required: [role]
      properties:
        role:
          type: string
          enum: [starter, interchange, reserve]

    PlayerSwap:
      type: object
      required: [playerInId, playerOutId]
      properties:
        playerInId:
          type: integer
        playerOutId:
          type: integer

    Contract:
      type: object
      required: [id, playerId, teamId, salary, startDate, endDate, bonusPerWin, status, createdAt]
      properties:
        id:
          type: integer
        playerId:
          type: integer
        teamId:
          type: integer
        salary:
          type: number
        startDate:
          type: string
        endDate:
          type: string
        bonusPerWin:
          type: number
        status:
          type: string
          enum: [active, expired, terminated]
        player:
          $ref: "#/components/schemas/Player"
        createdAt:
          type: string

    ContractInput:
      type: object
      required: [playerId, salary, endDate, bonusPerWin]
      properties:
        playerId:
          type: integer
        salary:
          type: number
        endDate:
          type: string
        bonusPerWin:
          type: number

    StaffMember:
      type: object
      required: [id, name, role, specialty, salary, skillLevel, age, overallRating, contractLength, coachSpeciality, personality, attributes, specialTrait, isScoutRevealed, scoutingRating, teamId, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        role:
          type: string
          enum: [head_coach, assistant_coach, fitness_trainer, strength_conditioner, massage_therapist, promotions_manager]
        specialty:
          type: string
        salary:
          type: number
        skillLevel:
          type: integer
        age:
          type: integer
        overallRating:
          type: integer
          description: "Staff quality rating 50–99."
        contractLength:
          type: integer
          description: "Contract duration in months."
        coachSpeciality:
          type: string
          enum: [Technical, Athletic, Defensive, Conditioning, "Youth Development", General]
          description: "Determines which stats get a training bonus."
        personality:
          type: string
          enum: [Motivator, Demanding, "Player Friendly", Disciplinarian]
          description: "Affects XP multiplier and morale/fatigue side-effects."
        attributes:
          type: object
          additionalProperties:
            type: integer
          description: "3 role-specific attributes (name → value 1–99)."
        specialTrait:
          type: string
          description: "Unique special trait for this staff member."
        isScoutRevealed:
          type: boolean
          description: "Whether OVR has been revealed by a scout."
        scoutingRating:
          type: integer
          description: "Scouting effectiveness rating 1–100."
        teamId:
          type: ["integer", "null"]
        nationality:
          type: ["string", "null"]
        imageUrl:
          type: ["string", "null"]
        createdAt:
          type: string

    StaffInput:
      type: object
      required: [staffId]
      properties:
        staffId:
          type: integer

    StaffMarketFilters:
      type: object
      properties:
        role:
          type: string
        search:
          type: string

    TrainingSession:
      type: object
      required: [id, teamId, playerId, type, focus, durationHours, scheduledAt, status, coachId]
      properties:
        id:
          type: integer
        teamId:
          type: integer
        playerId:
          type: integer
        type:
          type: string
          enum: ["Power Camp", "Agility Camp", "Serving Academy", "Defensive Systems", "Conditioning", "Recovery Program"]
        focus:
          type: string
        durationHours:
          type: number
        scheduledAt:
          type: string
        status:
          type: string
          enum: [scheduled, completed, cancelled]
        coachId:
          type: ["integer", "null"]
        player:
          $ref: "#/components/schemas/Player"
        coach:
          $ref: "#/components/schemas/StaffMember"
        createdAt:
          type: string

    TrainingInput:
      type: object
      required: [playerId, type, durationHours, scheduledAt]
      properties:
        playerId:
          type: integer
        type:
          type: string
          enum: ["Power Camp", "Agility Camp", "Serving Academy", "Defensive Systems", "Conditioning", "Recovery Program"]
        focus:
          type: string
        durationHours:
          type: number
        scheduledAt:
          type: string
        coachId:
          type: ["integer", "null"]

    TrainingResult:
      type: object
      required: [session, statGains, newStats]
      properties:
        session:
          $ref: "#/components/schemas/TrainingSession"
        statGains:
          type: object
          properties:
            speed: { type: integer }
            power: { type: integer }
            defense: { type: integer }
            serve: { type: integer }
            block: { type: integer }
            stamina: { type: integer }
        newStats:
          $ref: "#/components/schemas/Player"
        xpGained:
          type: integer
        baseXp:
          type: integer
        xpToNextStat:
          type: integer
        ageModifier:
          type: number
        philosophyMultiplier:
          type: number
        programName:
          type: string
        coachEffect:
          type: object
          nullable: true

    TeamTrainingInput:
      type: object
      required: [type, durationHours, scheduledAt]
      properties:
        type:
          type: string
          enum: ["Power Camp", "Agility Camp", "Serving Academy", "Defensive Systems", "Conditioning", "Recovery Program"]
        focus:
          type: string
        durationHours:
          type: number
        scheduledAt:
          type: string
        coachId:
          type: ["integer", "null"]

    TrainingPlan:
      type: object
      required: [weeklyLoad, averageFitness, averageMorale, scheduledSessions, completedThisWeek]
      properties:
        weeklyLoad:
          type: string
          enum: [light, moderate, intense, peak]
        averageFitness:
          type: number
        averageMorale:
          type: number
        averageFatigue:
          type: number
        scheduledSessions:
          type: array
          items:
            $ref: "#/components/schemas/TrainingSession"
        completedThisWeek:
          type: integer

    Match:
      type: object
      required: [id, homeTeamId, awayTeamId, locationId, weather, status, season, round, teamSize, createdAt]
      properties:
        id:
          type: integer
        homeTeamId:
          type: integer
        awayTeamId:
          type: integer
        locationId:
          type: integer
        weather:
          type: string
          enum: [sunny, cloudy, windy, hot, overcast, stormy, perfect]
        windSpeed:
          type: ["number", "null"]
        temperature:
          type: ["number", "null"]
        status:
          type: string
          enum: [scheduled, in_progress, completed, cancelled]
        season:
          type: integer
        round:
          type: integer
        teamSize:
          type: integer
        homeScore:
          type: ["integer", "null"]
        awayScore:
          type: ["integer", "null"]
        homeTeamName:
          type: ["string", "null"]
        awayTeamName:
          type: ["string", "null"]
        locationName:
          type: ["string", "null"]
        prizeAmount:
          type: ["number", "null"]
        scheduledAt:
          type: ["string", "null"]
        lineup:
          type: ["array", "null"]
          items:
            type: integer
        createdAt:
          type: string

    MatchInput:
      type: object
      required: [awayTeamId, locationId, season, round, teamSize, scheduledAt]
      properties:
        awayTeamId:
          type: integer
        locationId:
          type: integer
        season:
          type: integer
        round:
          type: integer
        teamSize:
          type: integer
        scheduledAt:
          type: string
        prizeAmount:
          type: number

    MatchResult:
      type: object
      required: [match, highlights, homeScore, awayScore, winner, prizeEarned]
      properties:
        match:
          $ref: "#/components/schemas/Match"
        highlights:
          type: array
          items:
            type: string
        homeScore:
          type: integer
        awayScore:
          type: integer
        winner:
          type: string
          enum: [home, away, draw]
        prizeEarned:
          type: number
        mvp:
          $ref: "#/components/schemas/Player"
        playerEvents:
          type: array
          description: Injury and recovery events that occurred as a result of this match.
          items:
            type: object
            required: [playerId, playerName, event]
            properties:
              playerId:
                type: integer
              playerName:
                type: string
              event:
                type: string
                enum: [injury_new, injury_worsened, recovery_complete]
              injuryStatus:
                type: string
              weeksOut:
                type: integer

    LineupUpdate:
      type: object
      required: [playerIds]
      properties:
        playerIds:
          type: array
          items:
            type: integer

    Season:
      type: object
      required: [id, year, name, status, totalRounds, currentRound, startDate, endDate]
      properties:
        id:
          type: integer
        year:
          type: integer
        name:
          type: string
        status:
          type: string
          enum: [upcoming, active, completed]
        totalRounds:
          type: integer
        currentRound:
          type: integer
        startDate:
          type: string
        endDate:
          type: string

    SeasonInput:
      type: object
      required: [year, name, totalRounds, startDate, endDate]
      properties:
        year:
          type: integer
        name:
          type: string
        totalRounds:
          type: integer
        startDate:
          type: string
        endDate:
          type: string

    LadderEntry:
      type: object
      required: [rank, teamId, teamName, wins, losses, points, goalsFor, goalsAgainst]
      properties:
        rank:
          type: integer
        teamId:
          type: integer
        teamName:
          type: string
        wins:
          type: integer
        losses:
          type: integer
        points:
          type: integer
        goalsFor:
          type: integer
        goalsAgainst:
          type: integer

    FinanceTransaction:
      type: object
      required: [id, teamId, type, amount, description, category, date, createdAt]
      properties:
        id:
          type: integer
        teamId:
          type: integer
        type:
          type: string
          enum: [income, expense]
        amount:
          type: number
        description:
          type: string
        category:
          type: string
          enum: [prize_money, sponsorship, promo_deal, player_salary, staff_salary, transfer_fee, training_cost, outfit_purchase, other]
        date:
          type: string
        createdAt:
          type: string

    FinanceTransactionInput:
      type: object
      required: [type, amount, description, category, date]
      properties:
        type:
          type: string
        amount:
          type: number
        description:
          type: string
        category:
          type: string
        date:
          type: string

    FinanceSummary:
      type: object
      required: [totalBalance, totalIncome, totalExpenses, monthlyIncome, monthlyExpenses, incomeSources, expenseBreakdown, recentTransactions]
      properties:
        totalBalance:
          type: number
        totalIncome:
          type: number
        totalExpenses:
          type: number
        monthlyIncome:
          type: number
        monthlyExpenses:
          type: number
        incomeSources:
          type: object
          properties:
            prizeMoney: { type: number }
            sponsorships: { type: number }
            promoDeals: { type: number }
        expenseBreakdown:
          type: object
          properties:
            playerSalaries: { type: number }
            staffSalaries: { type: number }
            trainingCosts: { type: number }
            other: { type: number }
        recentTransactions:
          type: array
          items:
            $ref: "#/components/schemas/FinanceTransaction"

    StaffWageBillMember:
      type: object
      required: [id, name, role, weeklySalary]
      properties:
        id:
          type: integer
        name:
          type: string
        role:
          type: string
        weeklySalary:
          type: number

    StaffWageBill:
      type: object
      required: [weeklyWages, monthlyWages, staffCount, staff]
      properties:
        weeklyWages:
          type: number
        monthlyWages:
          type: number
        staffCount:
          type: integer
        staff:
          type: array
          items:
            $ref: "#/components/schemas/StaffWageBillMember"

    SponsorReputation:
      type: object
      required: [score, label, stars]
      properties:
        score:
          type: integer
          minimum: 0
          maximum: 100
        label:
          type: string
        stars:
          type: integer
          minimum: 1
          maximum: 5

    WageBillPlayer:
      type: object
      required: [id, name, tier, weeklySalary]
      properties:
        id:
          type: integer
        name:
          type: string
        tier:
          type: string
        weeklySalary:
          type: number

    WageBill:
      type: object
      required: [weeklyWages, monthlyWages, playerCount, players]
      properties:
        weeklyWages:
          type: number
        monthlyWages:
          type: number
        playerCount:
          type: integer
        players:
          type: array
          items:
            $ref: "#/components/schemas/WageBillPlayer"

    PrizeMoneyCategory:
      type: object
      required: [category, amount, matches]
      properties:
        category:
          type: string
        amount:
          type: number
        matches:
          type: integer

    PrizeMoneySummary:
      type: object
      required: [total, breakdown]
      properties:
        total:
          type: number
        breakdown:
          type: array
          items:
            $ref: "#/components/schemas/PrizeMoneyCategory"

    SponsorProgress:
      type: object
      required: [id, sponsor, description, amount, requirementWins, expiresAt, imageUrl, currentWins, progressPct, isComplete]
      properties:
        id:
          type: integer
        sponsor:
          type: string
        description:
          type: string
        amount:
          type: number
        requirementWins:
          type: integer
        expiresAt:
          type: string
        imageUrl:
          type: ["string", "null"]
        currentWins:
          type: integer
        progressPct:
          type: integer
        isComplete:
          type: boolean

    PromoDeal:
      type: object
      required: [id, sponsor, description, amount, requirementWins, expiresAt, isAccepted]
      properties:
        id:
          type: integer
        sponsor:
          type: string
        description:
          type: string
        amount:
          type: number
        requirementWins:
          type: integer
        expiresAt:
          type: string
        isAccepted:
          type: boolean
        imageUrl:
          type: ["string", "null"]

    DraftPlayer:
      type: object
      required: [id, name, nationality, age, height, position, speed, power, defense, serve, block, stamina, askingPrice, available]
      properties:
        id:
          type: integer
        name:
          type: string
        nationality:
          type: string
        age:
          type: integer
        height:
          type: number
        position:
          type: string
        speed:
          type: integer
        power:
          type: integer
        defense:
          type: integer
        serve:
          type: integer
        block:
          type: integer
        stamina:
          type: integer
        askingPrice:
          type: number
        available:
          type: boolean
        imageUrl:
          type: ["string", "null"]

    DraftPickInput:
      type: object
      required: [draftPlayerId]
      properties:
        draftPlayerId:
          type: integer

    OlympicPlayer:
      type: object
      required: [name, nationality, age, speed, power, defense, serve, block, stamina, isReserve]
      properties:
        id:
          type: ["integer", "null"]
        name:
          type: string
        nationality:
          type: string
        age:
          type: integer
        speed:
          type: integer
        power:
          type: integer
        defense:
          type: integer
        serve:
          type: integer
        block:
          type: integer
        stamina:
          type: integer
        isReserve:
          type: boolean
        imageUrl:
          type: ["string", "null"]

    OlympicCountry:
      type: object
      required: [country, flag, playerCount, squad]
      properties:
        country:
          type: string
        flag:
          type: string
        playerCount:
          type: integer
        squad:
          type: array
          items:
            $ref: "#/components/schemas/OlympicPlayer"

    OlympicSelection:
      type: object
      required: [country, flag, squad]
      properties:
        country:
          type: string
        flag:
          type: string
        squad:
          type: array
          items:
            $ref: "#/components/schemas/OlympicPlayer"

    OlympicSelectionInput:
      type: object
      required: [country, flag, squad]
      properties:
        country:
          type: string
        flag:
          type: string
        squad:
          type: array
          items:
            $ref: "#/components/schemas/OlympicPlayer"

    ScoutingResult:
      type: object
      required: [player, scoutedPotential, confidence, scoutName, scoutRating]
      properties:
        player:
          $ref: "#/components/schemas/Player"
        scoutedPotential:
          type: string
          enum: [Low, Average, High, Elite, Generational]
        confidence:
          type: string
          enum: [uncertain, likely, confident]
          description: How reliable the assessment is, based on scout skill.
        scoutName:
          type: string
        scoutRating:
          type: integer

    TrophyRecord:
      type: object
      required:
        - id
        - type
        - name
      properties:
        id:
          type: integer
        type:
          type: string
        name:
          type: string
        season:
          type: integer
          nullable: true
        year:
          type: integer
          nullable: true
        continent:
          type: string
          nullable: true
        locationName:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true

    ClubHonours:
      type: object
      required:
        - worldChampionships
        - continentalChampionships
        - continentalFinalsByContinent
        - grandFinals
        - runnerUps
        - bronzes
      properties:
        worldChampionships:
          type: array
          items:
            $ref: "#/components/schemas/TrophyRecord"
        continentalChampionships:
          type: array
          items:
            $ref: "#/components/schemas/TrophyRecord"
        continentalFinalsByContinent:
          type: object
          additionalProperties:
            type: integer
        grandFinals:
          type: array
          items:
            $ref: "#/components/schemas/TrophyRecord"
        runnerUps:
          type: array
          items:
            $ref: "#/components/schemas/TrophyRecord"
        bronzes:
          type: array
          items:
            $ref: "#/components/schemas/TrophyRecord"

    OlympicMedalRecord:
      type: object
      required:
        - gold
        - silver
        - bronze
        - appearances
        - hosts
      properties:
        gold:
          type: integer
        silver:
          type: integer
        bronze:
          type: integer
        appearances:
          type: integer
        hosts:
          type: array
          items:
            type: object
            required:
              - year
              - location
            properties:
              year:
                type: integer
              location:
                type: string

    Achievement:
      type: object
      required:
        - id
        - title
        - description
        - icon
        - tier
        - unlocked
        - progress
        - target
      properties:
        id:
          type: string
        title:
          type: string
        description:
          type: string
        icon:
          type: string
        tier:
          type: string
          enum:
            - bronze
            - silver
            - gold
            - platinum
        unlocked:
          type: boolean
        progress:
          type: integer
        target:
          type: integer

    ManagerRecords:
      type: object
      required:
        - mostTitles
        - mostWins
        - bestWinStreak
        - mostPrizeMoney
        - seasonsManaged
        - olympicMedals
      properties:
        mostTitles:
          type: integer
        mostWins:
          type: integer
        bestWinStreak:
          type: integer
        mostPrizeMoney:
          type: string
        seasonsManaged:
          type: integer
        olympicMedals:
          type: integer

    TrophyCabinet:
      type: object
      required:
        - honours
        - olympicMedals
        - achievements
        - records
      properties:
        honours:
          $ref: "#/components/schemas/ClubHonours"
        olympicMedals:
          $ref: "#/components/schemas/OlympicMedalRecord"
        achievements:
          type: array
          items:
            $ref: "#/components/schemas/Achievement"
        records:
          $ref: "#/components/schemas/ManagerRecords"

    Facility:
      type: object
      required:
        - id
        - teamId
        - type
        - level
      properties:
        id:
          type: integer
        teamId:
          type: integer
        type:
          type: string
        level:
          type: integer

    ClubRatingComponent:
      type: object
      required: [score, weight, contribution, detail]
      properties:
        score:
          type: integer
        weight:
          type: integer
        contribution:
          type: integer
        detail:
          type: string

    ClubRating:
      type: object
      required: [totalRating, label, breakdown]
      properties:
        totalRating:
          type: integer
        label:
          type: string
        breakdown:
          type: object
          required: [players, staff, medical, facilities, youthAcademy]
          properties:
            players:
              $ref: "#/components/schemas/ClubRatingComponent"
            staff:
              $ref: "#/components/schemas/ClubRatingComponent"
            medical:
              $ref: "#/components/schemas/ClubRatingComponent"
            facilities:
              $ref: "#/components/schemas/ClubRatingComponent"
            youthAcademy:
              $ref: "#/components/schemas/ClubRatingComponent"

    RunCampInput:
      type: object
      required:
        - campType
      properties:
        campType:
          type: string

    WellbeingEffect:
      type: object
      required:
        - id
        - effectType
        - matchesRemaining
        - createdAt
      properties:
        id:
          type: integer
        effectType:
          type: string
        matchesRemaining:
          type: integer
        createdAt:
          type: string

    WellbeingStatus:
      type: object
      required:
        - teamBudget
        - activeEffects
      properties:
        teamBudget:
          type: number
        activeEffects:
          type: array
          items:
            $ref: "#/components/schemas/WellbeingEffect"

    WellbeingResult:
      type: object
      required:
        - message
        - updatedPlayers
        - activeEffects
      properties:
        message:
          type: string
        updatedPlayers:
          type: integer
        activeEffects:
          type: array
          items:
            $ref: "#/components/schemas/WellbeingEffect"

    HallOfFameEntry:
      type: object
      required:
        - id
        - name
        - nationality
        - position
        - peakOverallRating
        - careerSeasons
        - careerWins
        - careerTitles
        - continentalTitles
        - worldTitles
        - olympicMedalsCount
        - legendScore
      properties:
        id:
          type: integer
        name:
          type: string
        nationality:
          type: string
        position:
          type: string
        imageUrl:
          type: string
          nullable: true
        peakOverallRating:
          type: integer
        careerSeasons:
          type: integer
        careerWins:
          type: integer
        careerTitles:
          type: integer
        continentalTitles:
          type: integer
        worldTitles:
          type: integer
        olympicMedalsCount:
          type: integer
        retiredSeasonYear:
          type: integer
          nullable: true
        yearsActive:
          type: string
          nullable: true
        legendScore:
          type: integer

    LeaderboardEntry:
      type: object
      required: [rank, teamId, teamName, userId, wins, losses, earnings, reputation]
      properties:
        rank:
          type: integer
        teamId:
          type: integer
        teamName:
          type: string
        userId:
          type: string
        username:
          type: string
        wins:
          type: integer
        losses:
          type: integer
        earnings:
          type: number
        reputation:
          type: integer

    InjuryHistoryEntry:
      type: object
      required: [id, teamId, seasonId, playerId, playerName, injuryType, daysMissed, dateInjured]
      properties:
        id:
          type: integer
        teamId:
          type: integer
        seasonId:
          type: integer
        playerId:
          type: integer
        playerName:
          type: string
        injuryType:
          type: string
        daysMissed:
          type: integer
        dateInjured:
          type: string
          format: date-time

    PlayerWorkload:
      type: object
      required: [id, name, position, squadRole, matchesPlayed, trainingSessions, status]
      properties:
        id:
          type: integer
        name:
          type: string
        position:
          type: string
        squadRole:
          type: string
        imageUrl:
          type: string
          nullable: true
        matchesPlayed:
          type: integer
        trainingSessions:
          type: integer
        status:
          type: string
          enum: [Fresh, "Heavy Load", Overworked]

    SeasonInjuryStats:
      type: object
      required: [seasonId, totalInjuries, daysLost, minorInjuries, majorInjuries, unavailableInjuries, avgRecoveryDays, mostCommon, currentInjuryCount]
      properties:
        seasonId:
          type: integer
        totalInjuries:
          type: integer
        daysLost:
          type: integer
        minorInjuries:
          type: integer
        majorInjuries:
          type: integer
        unavailableInjuries:
          type: integer
        avgRecoveryDays:
          type: integer
        mostCommon:
          type: string
        currentInjuryCount:
          type: integer

    YouthLadderEntry:
      type: object
      required: [id, teamId, season, competitorName, isPlayer, wins, losses, points]
      properties:
        id:
          type: integer
        teamId:
          type: integer
        season:
          type: integer
        competitorName:
          type: string
        isPlayer:
          type: boolean
        wins:
          type: integer
        losses:
          type: integer
        points:
          type: integer
        createdAt:
          type: string

    YouthStar:
      type: object
      required: [id, name, age, rating, potential, speciality, nationality]
      properties:
        id:
          type: integer
        name:
          type: string
        age:
          type: integer
        rating:
          type: integer
        potential:
          type: string
        speciality:
          type: string
        nationality:
          type: string

    YouthChampionshipTrophy:
      type: object
      required: [id, teamId, season, winningTeamName, isPlayerWin]
      properties:
        id:
          type: integer
        teamId:
          type: integer
        season:
          type: integer
        year:
          type: integer
          nullable: true
        winningTeamName:
          type: string
        isPlayerWin:
          type: boolean
        createdAt:
          type: string

    YouthLeagueResult:
      type: object
      required: [id, teamId, playerId, playerName, weekNumber, result, oppositionName, xpGained, devPointsGained, moraleChange, playerRatingAtTime, createdAt]
      properties:
        id:
          type: integer
        teamId:
          type: integer
        playerId:
          type: integer
        playerName:
          type: string
        weekNumber:
          type: integer
        result:
          type: string
          enum: [win, loss, draw]
        oppositionName:
          type: string
        xpGained:
          type: integer
        devPointsGained:
          type: integer
        moraleChange:
          type: integer
        playerRatingAtTime:
          type: integer
        createdAt:
          type: string

    StartYouthScoutingInput:
      type: object
      required: [continent]
      properties:
        continent:
          type: string

    ContinentalScoutingMission:
      type: object
      required: [id, status, durationMonths, startDate, endDate, cost, prospectsFound]
      properties:
        id:
          type: integer
        status:
          type: string
          enum: [active, completed, collected, cancelled]
        durationMonths:
          type: integer
        startDate:
          type: string
        endDate:
          type: string
        assignedStaffId:
          type: ["integer", "null"]
        cost:
          type: number
        prospectsFound:
          type: integer

    ContinentalRegion:
      type: object
      required: [id, name, emoji, specialties, description, talentLevel, talentColor, costs]
      properties:
        id:
          type: string
        name:
          type: string
        emoji:
          type: string
        specialties:
          type: array
          items:
            type: string
        description:
          type: string
        talentLevel:
          type: string
        talentColor:
          type: string
        costs:
          type: object
          additionalProperties:
            type: integer
        activeMission:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/ContinentalScoutingMission"

    StartContinentalScoutingInput:
      type: object
      required: [region, durationMonths]
      properties:
        region:
          type: string
        durationMonths:
          type: integer
          enum: [1, 3, 6]
        staffId:
          type: ["integer", "null"]

    CollectMissionResult:
      type: object
      required: [prospectsFound, scoutingRating, prospects]
      properties:
        prospectsFound:
          type: integer
        scoutName:
          type: ["string", "null"]
        scoutingRating:
          type: integer
        prospects:
          type: array
          items:
            $ref: "#/components/schemas/YouthProspect"

    YouthProspect:
      type: object
      required: [id, name, age, continent, currentRating, potentialStars, speciality, signingCost, status]
      properties:
        id:
          type: integer
        name:
          type: string
        age:
          type: integer
        continent:
          type: string
        currentRating:
          type: integer
        potentialStars:
          type: string
        speciality:
          type: string
        signingCost:
          type: integer
        status:
          type: string
          enum: [pending, signed, ignored]
        scoutingReportText:
          type: ["string", "null"]
          description: "Scout's written narrative assessment."
        discoveredBy:
          type: ["string", "null"]
          description: "Discovery attribution text."
        scoutedPotentialLabel:
          type: ["string", "null"]
          enum: ["Very Low", "Low", "Moderate", "High", "Elite", null]
          description: "Scout's assessed potential label (may be inaccurate for poor scouts)."
        continentalMissionId:
          type: ["integer", "null"]
          description: "ID of the continental mission that found this prospect."
        eliteEventType:
          type: ["string", "null"]
          enum: ["Generational Talent", "Olympic Wonderkid", "Physical Freak", "Local Hero", null]
          description: "Rare elite scouting event classification, if applicable."

    YouthScoutingMission:
      type: object
      required: [status, weeksRemaining, scoutingCost]
      properties:
        status:
          type: string
          enum: [idle, active, complete]
        continent:
          type: string
          nullable: true
        weeksRemaining:
          type: integer
        expectedTalentLevel:
          type: string
          nullable: true
        scoutingCost:
          type: integer
          description: Fixed cost in dollars to send scouts on a mission

    Dashboard:
      type: object
      required: [team, nextMatch, financeSummary, recentResults, topPlayers, seasonStanding, injuredCount]
      properties:
        team:
          $ref: "#/components/schemas/Team"
        nextMatch:
          type: object
          nullable: true
        financeSummary:
          type: object
          properties:
            balance: { type: number }
            monthlyNet: { type: number }
        recentResults:
          type: array
          items:
            $ref: "#/components/schemas/Match"
        topPlayers:
          type: array
          items:
            $ref: "#/components/schemas/Player"
        seasonStanding:
          type: object
          nullable: true
          properties:
            rank: { type: integer }
            wins: { type: integer }
            losses: { type: integer }
            points: { type: integer }
        injuredCount:
          type: integer

    AchievementStatus:
      type: object
      required:
        - key
        - name
        - description
        - category
        - unlocked
        - progress
      properties:
        key:
          type: string
        name:
          type: string
        description:
          type: string
        category:
          type: string
          enum: [career, finance, youth, competition, legacy]
        unlocked:
          type: boolean
        unlockedAt:
          type: string
          nullable: true
        seasonUnlocked:
          type: integer
          nullable: true
        progress:
          type: object
          required:
            - current
            - target
          properties:
            current:
              type: number
            target:
              type: number

    CareerStats:
      type: object
      properties:
        matchesWon:
          type: integer
        championshipsWon:
          type: integer
        highestBalanceReached:
          type: number
        seasonsCompleted:
          type: integer
        seasonsInCurrentLocation:
          type: integer
        continentsVisited:
          type: array
          items:
            type: string
        youthSigned:
          type: integer
        youthPromoted:
          type: integer
        playersDevelopedToFiveStar:
          type: integer
        continentalTitles:
          type: integer
        olympicGolds:
          type: integer
        perfectSeasons:
          type: integer
        debtFreeSeasons:
          type: integer
